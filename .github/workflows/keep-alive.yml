name: Keep Backend Alive

on:
  schedule:
    # Esegue ogni 15 minuti per mantenere Render sveglio
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  ping-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: |
          echo "🔔 Risveglio backend Render..."
          curl -f https://newsflow-backend-v2.onrender.com/api/health || echo "Backend in risveglio..."
          echo "✅ Ping completato"

