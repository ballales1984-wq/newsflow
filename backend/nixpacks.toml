[phases.setup]
nixPkgs = ["python311", "pip"]

[phases.install]
cmds = ["pip install -r requirements-minimal.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn app.main_simple:app --host 0.0.0.0 --port $PORT"

